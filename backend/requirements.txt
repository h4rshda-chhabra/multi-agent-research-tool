# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0          # SQLite async driver (local dev)
asyncpg==0.30.0            # PostgreSQL async driver (production)

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Config
pydantic==2.10.3
email-validator==2.2.0
pydantic-settings==2.6.1

# AI / Agents
openai==1.35.0
google-generativeai>=0.7.0
langgraph==0.2.60
langchain-core==0.3.25
tavily-python==0.5.0
arxiv==2.1.3

# HTTP
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.4.0

# PDF Export
# weasyprint is intentionally omitted — it requires GTK which is unavailable
# in a slim Linux container. fpdf2 handles PDF generation; HTML is the fallback.
fpdf2==2.8.7           # pure-Python PDF; no external DLLs
markdown==3.7

# Utilities
python-dotenv==1.0.1
structlog==24.4.0
tenacity==9.0.0
